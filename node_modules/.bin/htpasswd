#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../htpasswd/bin/htpasswd" "$@"
  ret=$?
else 
  node  "$basedir/../htpasswd/bin/htpasswd" "$@"
  ret=$?
fi
exit $ret
