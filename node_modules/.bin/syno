#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../syno/bin/syno.js" "$@"
  ret=$?
else 
  node  "$basedir/../syno/bin/syno.js" "$@"
  ret=$?
fi
exit $ret
