#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../strong-deploy/bin/sl-deploy.js" "$@"
  ret=$?
else 
  node  "$basedir/../strong-deploy/bin/sl-deploy.js" "$@"
  ret=$?
fi
exit $ret
