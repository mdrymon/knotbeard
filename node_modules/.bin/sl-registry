#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../strong-registry/bin/sl-registry.js" "$@"
  ret=$?
else 
  node  "$basedir/../strong-registry/bin/sl-registry.js" "$@"
  ret=$?
fi
exit $ret
