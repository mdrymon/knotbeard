#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../strong-npm-ls/bin/sl-npm-ls.js" "$@"
  ret=$?
else 
  node  "$basedir/../strong-npm-ls/bin/sl-npm-ls.js" "$@"
  ret=$?
fi
exit $ret
