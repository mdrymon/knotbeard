#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../strong-nginx-controller/bin/sl-nginx-ctl.js" "$@"
  ret=$?
else 
  node  "$basedir/../strong-nginx-controller/bin/sl-nginx-ctl.js" "$@"
  ret=$?
fi
exit $ret
