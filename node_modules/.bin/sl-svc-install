#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../strong-service-install/bin/sl-svc-install.js" "$@"
  ret=$?
else 
  node  "$basedir/../strong-service-install/bin/sl-svc-install.js" "$@"
  ret=$?
fi
exit $ret
