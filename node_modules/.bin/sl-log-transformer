#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../strong-log-transformer/bin/sl-log-transformer.js" "$@"
  ret=$?
else 
  node  "$basedir/../strong-log-transformer/bin/sl-log-transformer.js" "$@"
  ret=$?
fi
exit $ret
