#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../strong-docker-build/bin/sl-docker-build.js" "$@"
  ret=$?
else 
  node  "$basedir/../strong-docker-build/bin/sl-docker-build.js" "$@"
  ret=$?
fi
exit $ret
